{
    "rules": {
        "orders": {
            ".read": "auth != null",
            ".write": "auth != null",
            "$orderId": {
                ".read": true,
                ".write": "!data.exists() || auth != null"
            }
        }
    }
}